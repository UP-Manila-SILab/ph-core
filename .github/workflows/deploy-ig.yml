# Deploy workflow — DEPRECATED
# All deployment logic has been merged into ig-release.yml.
# This file is kept as a placeholder for backward compatibility.
# TODO: Remove this file after confirming ig-release.yml handles all cases.

name: Deploy PH Core FHIR IG

on:
  workflow_run:
    workflows: ["Build PH Core FHIR IG"]
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.event.workflow_run.conclusion == 'success'
    steps:
      - name: Deployment handled by ig-release.yml
        run: |
          echo "DEPRECATED: deploy-ig.yml no longer deploys."
          echo "ig-release.yml handles all gh-pages deployment."
          echo "Delete this workflow once confirmed working."
